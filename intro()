# vectors in R
# A vector is an object that has both a magnitude and a direction.

# rnorm() generates a vector of normally distributed random numbers
x <- rnorm(1000, mean=10)

# min(), is used to calculate the minimum of vector elements
min(x)

# sd(),is used to compute the standard deviation of given values
sd(x)

# seq Generate regular sequences

